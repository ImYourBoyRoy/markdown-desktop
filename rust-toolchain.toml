[toolchain]
# CI pins the release/quality runners to 1.98.0. The local stable channel is
# used here because this workstation's explicit 1.98.0 rustup entry is
# rustc-only even though the stable alias provides the complete 1.98 toolchain.
channel = "stable"
profile = "minimal"
components = ["rustfmt", "clippy"]
