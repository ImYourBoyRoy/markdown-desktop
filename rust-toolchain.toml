[toolchain]
# Full upgrades refresh stable; Cargo.toml records the minimum Rust version.
# check:toolchains detects an installed compiler older than that floor.
channel = "stable"
profile = "minimal"
components = ["rustfmt", "clippy"]
