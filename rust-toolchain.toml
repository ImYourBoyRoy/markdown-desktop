[toolchain]
# Keep every Cargo invocation on the contract toolchain. CI installs this
# exact channel; using the moving stable alias here would allow a runner's
# older stable toolchain to override the CI action and fail rust-version.
channel = "1.98.0"
profile = "minimal"
components = ["rustfmt", "clippy"]
